# Archive

This folder keeps non-active source artifacts out of the project root.

- `original-html/`: original standalone HTML export kept for reference.
- `strapi-backend-experiment/`: earlier Strapi backend scaffold. The active backend is now `../backend/`, the PHP/MySQL CMS upload folder.
